export interface ToastObject<Data extends object = Record<string, unknown>> { id: string; title?: string; description?: string; type?: string; timeout?: number; priority?: 'low' | 'high'; transitionStatus?: 'starting' | 'ending'; updateKey?: number; limited?: boolean; onClose?: () => void; onRemove?: () => void; data?: Data; actionProps?: Record<string, unknown>; }
export type ToastManagerAddOptions<Data extends object = Record<string, unknown>> = Omit<ToastObject<Data>, 'id'> & { id?: string };
export type ToastManagerUpdateOptions<Data extends object = Record<string, unknown>> = Partial<Omit<ToastObject<Data>, 'id'>>;
export interface ToastManagerPromiseOptions<Value, Data extends object = Record<string, unknown>> { loading: string | ToastManagerUpdateOptions<Data>; success: string | ToastManagerUpdateOptions<Data> | ((value: Value) => string | ToastManagerUpdateOptions<Data>); error: string | ToastManagerUpdateOptions<Data> | ((error: unknown) => string | ToastManagerUpdateOptions<Data>); }
export type ToastManagerEvent = { action: 'add' | 'close' | 'update' | 'promise'; options: any };
export interface ToastManager<Data extends object = Record<string, unknown>> { ' subscribe': (listener: (event: ToastManagerEvent) => void) => () => void; add: (options: ToastManagerAddOptions<Data>) => string; close: (id?: string) => void; update: (id: string, options: ToastManagerUpdateOptions<Data>) => void; promise: <Value>(promise: Promise<Value>, options: ToastManagerPromiseOptions<Value, Data>) => Promise<Value>; }
let nextToastId = 0;
export function createToastManager<Data extends object = Record<string, unknown>>(): ToastManager<Data> { const listeners = new Set<(event: ToastManagerEvent) => void>(); const emit = (event: ToastManagerEvent) => listeners.forEach((listener) => listener(event)); return { ' subscribe'(listener) { listeners.add(listener); return () => listeners.delete(listener); }, add(options) { const id = options.id ?? `toast-${++nextToastId}`; emit({ action: 'add', options: { ...options, id } }); return id; }, close(id) { emit({ action: 'close', options: { id } }); }, update(id, options) { emit({ action: 'update', options: { ...options, id } }); }, promise(promise, options) { emit({ action: 'promise', options: { ...options, promise } }); return promise; } }; }

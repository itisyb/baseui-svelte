<script lang="ts">import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { getComboboxContext } from './combobox-context.svelte.js'; interface Props extends Omit<HTMLAttributes<HTMLDivElement>, 'children'> { children?: Snippet<[number]>; ref?: HTMLDivElement | null; } let { children, ref = $bindable(null), ...rest }: Props = $props(); const box = getComboboxContext();</script><div bind:this={ref} {...rest} role="status" aria-live="polite" style="position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)">{#if children}{@render children(box.visibleItems.length)}{:else}{box.visibleItems.length} result{box.visibleItems.length === 1 ? '' : 's'} available{/if}</div>
